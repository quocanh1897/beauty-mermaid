# Prompt

This project name: beauty-mermaid

We need to implement a webpage look like https://mermaid.live ([screenshot here](/mermaid.live.png) need to clone the editor code at https://github.com/mermaid-js/mermaid-live-editor) but using this library to generate the chart: https://github.com/lukilabs/beautiful-mermaid. Add 1 more button to choose a result style as svg or ASCII chart.

Make a completed specs for the project first (you need to ask me questions to clarify the requirements), then make a design document for it, finally the detail implementation plan.

During the process, you need to open the browser to verify the result until it match with the expectation.
Before you finish the work, compare the result with the webpage (screenshot) again to make sure it satisfy all the requirements. After that, dockerize it and make the document to let a dev could deploy it in a self-hosted machine using docker compose.
